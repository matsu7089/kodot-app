---
import ListLayout from '@/layouts/ListLayout.astro'
import { getFirstPage } from '@/utils'

const page = await getFirstPage(Astro.url.pathname, 'notes')
---

<ListLayout primaryColor="green" title="Notes" page={page} />
